{
  "name": "strimchat",
  "version": "0.0.1",
  "description": "chat backend",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --exec 'node --require dotenv/config --require babel-register ./src/index.js'",
    "sync": "node --require 'dotenv/config' --require 'babel-register' ./src/sync.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/webpack/bin/webpack.js -p",
    "server": "node ./dist/main.js"
  },
  "author": "JP",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.6.1",
    "dotenv": "^5.0.1",
    "nodemon": "^1.17.2",
    "webpack": "^4.3.0",
    "webpack-cli": "^2.0.13"
  },
  "dependencies": {
    "pg": "^7.4.1",
    "sequelize": "^4.37.6",
    "ws": "^5.1.0"
  },
  "babel": {
    "presets": [
      ["@babel/preset-env", {
        "targets": {
          "node": "current"
        }
      }]
    ]
  }
}
